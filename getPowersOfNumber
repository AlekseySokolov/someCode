const getPowersOfNumber = (number, until) => {

 let result = [1, number];
 let numToArr = number;
  
  while(until > 1) {
    numToArr *= number;
    result.push(numToArr);
    until -= 1;
  };
  
 return result;
};
getPowersOfNumber(3, 3); => [ 1, 3, 9, 27 ];


