const objCopy = (obj) => {

  const result = {},
        keysAndValues = Object.entries(obj),
        map = new Map();

  keysAndValues.forEach(arr => map.set(arr[0], arr[1]));
  map.forEach((value, key) => result[key] = value);
  
  return result;
};
